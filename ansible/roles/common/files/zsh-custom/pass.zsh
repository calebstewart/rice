# Setup the default location for password store repository
export PASSWORD_STORE_DIR="$HOME/.local/share/password-store"

# Enable loading extension commands from the password store repo
export PASSWORD_STORE_ENABLE_EXTENSIONS=true
